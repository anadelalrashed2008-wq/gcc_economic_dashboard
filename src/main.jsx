import React from "react";
import ReactDOM from "react-dom/client";
import GCCDashboard from "./GCCDashboard.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GCCDashboard />
  </React.StrictMode>
);
